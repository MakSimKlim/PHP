<?xml version="1.0" encoding="UTF-8"?>
<configuration>
	<system.webServer>
		<handlers>
			<add name="BlockPHP" path="*.php" verb="*" preCondition="integratedMode" resourceType="File" requireAccess="None" />
			<!--	add: Указывает, что добавляется новый элемент в конфигурацию.
					name="BlockPHP": Имя правила, в данном случае правило называется BlockPHP.
					path="*.php": Указывает, что правило применяется ко всем файлам с расширением .php.
					verb="*": Указывает, что правило применяется для всех HTTP-методов (GET, POST и т.д.).
					preCondition="integratedMode": Указывает, что правило должно быть выполнено только в режиме интеграции (integrated mode).
					resourceType="File": Указывает, что правило применяется к файлам.
					requireAccess="None": Указывает, что доступ к указанным ресурсам не требуется.-->
		</handlers>
	</system.webServer>
</configuration>